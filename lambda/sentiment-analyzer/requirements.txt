# AWS SDK (通常はLambda環境に含まれているが、最新版を使用する場合)
boto3>=1.26.0
botocore>=1.29.0
gradio>=4.0.0
mangum==0.17.0
pandas==2.2.2
plotly==5.22.0
requests==2.31.0
transformers==4.41.2

# JSON処理とデータ操作
# 標準ライブラリのjsonを使用するため、追加のJSON関連パッケージは不要

# 日付・時刻処理
# 標準ライブラリのdatetimeを使用

# 正規表現処理
# 標準ライブラリのreを使用

# ログ処理
# 標準ライブラリのloggingを使用

# 型ヒント
# 標準ライブラリのtypingを使用（Python 3.7+）

# 軽量な自然言語処理用（オプション）
# textblob>=0.17.1  # 必要に応じてコメントアウト解除
# langdetect>=1.0.9  # 必要に応じてコメントアウト解除

# 数値計算（オプション）
# numpy>=1.21.0  # 必要に応じてコメントアウト解除

# 文字列処理の拡張（オプション）
# regex>=2023.12.25  # 必要に応じてコメントアウト解除
